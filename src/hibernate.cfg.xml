<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
          "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
          "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">
 
<!-- Generated by MyEclipse Hibernate Tools.                   -->
<hibernate-configuration>
 
    <session-factory>
        <!-- <property name="hbm2ddl.auto">update</property> -->
        
       
        <!--
        Hibernate access the database using SQL queries but every database such as Oracle, MySQL ,H2, etc have their own version
        of SQL queries and so hibernate needs dialect to convert database specific SQL statements
         -->
         <property name="dialect">org.hibernate.dialect.MySQLDialect</property>
         
         
         
        <property name="connection.url">jdbc:mysql://localhost:3306/feb2017</property>
        <property name="connection.username">root</property>
        <property name="connection.password">password</property>
        <property name="connection.driver_class">com.mysql.jdbc.Driver</property>
        
        
        <!-- 
        <property name="cache.provider_class">org.hibernate.cache.NoCacheProvider</property>
         -->
         
         <property name="cache.use_second_level_cache">true</property>
          <property name="cache.use_query_cache">true</property>
         <property name="cache.provider_class">org.hibernate.cache.EhCacheProvider</property>
         
         
        <property name="show_sql">true</property>
        
        <!--  Drop(if it available) and re-create(if its not available creates and new table with mapped columns) 
        	  the database schema on startup -->
       <!--  <property name="hbm2ddl.auto">create</property>
        --> 
        
        <!-- 
        	Now to create a table once and then update the table if already exists and also for the data be available whenever we want 
         -->
         <property name="hbm2ddl.auto">update</property>
        
        
        <!-- List out all the mode class that the you will use to create the model object(i.e the object that will store info you want to store in database) -->
        <mapping class="com.demo.model.UserDetails" />
        <mapping class="com.demo.model.Vehicle" />
        <mapping class="com.demo.model.TwoWheeler" />
        <mapping class="com.demo.model.FourWheeler" />
        
        
    </session-factory>
 
</hibernate-configuration>